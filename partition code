def partition(n,k):
    l=[]
    if k==1:
        return [[n]]
    else:
        for i in range(1,n):
            partition(n-i,k-1)
            l.append(partition(n-i,k-1))

    return l

print(partition(5,3))        

